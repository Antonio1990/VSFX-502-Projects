		collection void Ptc_Baking {        	drawmode all        	state closed	        attribute float cull:hidden {	        	description { when baking a point cloud set this to OFF ie. don't cull hidden surfaces }	        	label { cull:hidden }	        	detail varying	        	default 1	        	subtype switch	        	}	        attribute float cull:backfacing {	        	description { when baking a point cloud set this to OFF ie. don't cull backfacing surfaces }	        	label { cull:backfacing }	        	detail varying	        	default 1	        	subtype switch	        	}	         attribute float dice:rasterorient {	        	description { when baking a point cloud set this to OFF ie. view-independent dicing }	        	label { dice:rasterorient }	        	detail varying	        	default 1	        	subtype switch	        	}			}  	attribute string grouping:membership {		default {}		label {trace set}		description {Tags a surface(s) as a member of a ray tracing group}		}